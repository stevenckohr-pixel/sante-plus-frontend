name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Create package.json
        run: echo '{"name":"app","version":"1.0.0"}' > package.json
      
      - name: Install Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init "Sante Plus" "com.santeplus.app"
          npx cap add android
      
      - name: Copy web files to www
        run: |
          mkdir -p www
          cp index.html www/
          cp -r assets www/ 2>/dev/null || true
          cp -r core www/ 2>/dev/null || true
          cp -r modules www/ 2>/dev/null || true
      
      - name: Update capacitor config
        run: |
          echo '{
            "appId": "com.santeplus.app",
            "appName": "Sante Plus",
            "webDir": "www"
          }' > capacitor.config.json
      
      - name: Sync Capacitor
        run: npx cap sync android
      
      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SantePlus-App
          path: android/app/build/outputs/apk/debug/app-debug.apk
